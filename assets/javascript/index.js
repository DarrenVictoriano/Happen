var typed = new Typed("#typed-movement", {
    strings: ["changes", "decisions", "progress", "it"],
    backSpeed: 50,
    typeSpeed: 50
});

$("#login").on("click", function () {
    $("#login-modal").modal();
});